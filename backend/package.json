{
  "name": "fitplus-backend",
  "version": "0.1.0",
  "private": true,
  "description": "FitPlus AI proxy — holds the Anthropic key, generates and adapts structured workout plans, enforces programming guardrails.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/smoke.test.js"
  }
}
