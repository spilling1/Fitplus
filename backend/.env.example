# Copy to .env (or export these) before `npm start`.
# Without ANTHROPIC_API_KEY the backend runs in OFFLINE STUB MODE: it still
# generates valid, safe plans deterministically, but AI chat is disabled.

ANTHROPIC_API_KEY=

# Optional overrides
PORT=8080
FITPLUS_MODEL=claude-opus-4-8
